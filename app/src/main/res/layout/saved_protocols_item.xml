<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/displayPadding"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="@dimen/displayPadding"
        android:layout_marginBottom="4dp"
        android:background="@drawable/rounded_view"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        android:longClickable="true"
        android:paddingStart="@dimen/displayPadding"
        android:paddingTop="@dimen/displayPadding"
        android:paddingEnd="0dp"
        android:paddingBottom="@dimen/displayPadding">


        <TextView
            android:id="@+id/savedProtocolName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:fontFamily="@font/clear_sans_medium"
            android:text="Patient Name"
            android:textColor="@color/darkGrayText"
            android:textSize="@dimen/headerFont"
            app:layout_constraintEnd_toStartOf="@+id/editProtocol"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/savedProtocolType"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:fontFamily="@font/clear_sans_medium"
            android:text="Protocol Type"
            android:textColor="@color/lowGray"
            android:textSize="@dimen/hintFont"
            app:layout_constraintEnd_toEndOf="@+id/savedProtocolName"
            app:layout_constraintStart_toStartOf="@+id/savedProtocolName"
            app:layout_constraintTop_toBottomOf="@+id/savedProtocolName" />

        <ImageView
            android:id="@+id/editProtocol"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/actionBarItemBackground"
            android:clickable="true"
            android:focusable="true"
            android:padding="8dp"
            app:layout_constraintBottom_toBottomOf="@+id/uploadProtocol"
            app:layout_constraintEnd_toStartOf="@+id/uploadProtocol"
            app:srcCompat="@drawable/ic_edit" />

        <ImageView
            android:id="@+id/uploadProtocol"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginEnd="8dp"
            android:padding="8dp"
            android:background="?attr/actionBarItemBackground"
            android:focusable="true"
            android:clickable="true"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_upload" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>